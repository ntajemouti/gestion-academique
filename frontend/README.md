# Technology Stack

This project uses the following technology stack:

- Vite

- TypeScript

- React

- shadcn-ui

- Tailwind CSS

# Setup

You need to install dependencies using `npm i` and then use `npm run lint & npx tsc --noEmit -p tsconfig.app.json --strict` to check and fix any issues.
